{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/dmnk-rdr/open-jersey-db/schema/team.schema.json",
  "title": "Team",
  "description": "A club or national team that wears jerseys. One file per team under data/teams/<sport>/<league|national>/<slug>.yaml",
  "type": "object",
  "additionalProperties": false,
  "required": ["slug", "name", "sport", "type"],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Stable identifier, must equal the file name (without .yaml)."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical display name, e.g. 'FC Bayern München'."
    },
    "sport": {
      "enum": ["football", "basketball", "american-football", "ice-hockey", "baseball", "handball", "rugby"]
    },
    "type": {
      "enum": ["club", "national"]
    },
    "league": {
      "type": ["string", "null"],
      "pattern": "^[a-z0-9-]+$",
      "description": "League slug (e.g. 'bundesliga', 'nba', 'nfl', 'nhl'). null for national teams."
    },
    "country": {
      "type": ["string", "null"],
      "pattern": "^[A-Z]{2}(-[A-Z]{2,3})?$",
      "description": "ISO 3166-1 alpha-2 (optionally with subdivision, e.g. 'GB-ENG')."
    },
    "aliases": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Spelling variants used by shops/feeds. Drives the consumer's name normalization."
    },
    "founded": { "type": ["integer", "null"] },
    "city": {
      "type": ["string", "null"],
      "description": "Home city, e.g. 'Mannheim'. For clubs only; null/omitted for national teams."
    },
    "stadium": {
      "type": ["string", "null"],
      "description": "Home ground / arena, e.g. 'Carl-Benz-Stadion'. For clubs only."
    },
    "kit_suppliers": {
      "type": ["array", "null"],
      "description": "Kit/manufacturer history, most recent first. from/to = years; only 'from' = 'since', only 'to' = 'until'.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["manufacturer"],
        "properties": {
          "manufacturer": { "type": "string", "minLength": 1 },
          "from": { "type": ["integer", "null"] },
          "to": { "type": ["integer", "null"] }
        }
      }
    },
    "wikidata": {
      "type": ["string", "null"],
      "pattern": "^Q[0-9]+$",
      "description": "Wikidata item id (e.g. 'Q15789'), enables idempotent re-imports."
    },
    "achievements": {
      "type": ["array", "null"],
      "description": "Kuratierte, belegte sportliche Erfolge/Meilensteine (Titel, Turnier-Endrunden, Rekorde), jüngste zuerst. Wie kit_suppliers: nur handverifizierte Fakten aus einer verlässlichen Quelle (i. d. R. Wikipedia) — Wikidata modelliert Erfolge/Titel für Nationalteams nicht sauber genug für Automatisierung, also NICHT automatisiert ableiten.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["text"],
        "properties": {
          "year": {
            "type": ["string", "null"],
            "description": "Jahr oder Zeitraum, z. B. '2016' oder '1983–1990'."
          },
          "text": { "type": "string", "minLength": 1 }
        }
      }
    },
    "logo_url": {
      "type": ["string", "null"],
      "description": "Reference URL only — this database stores no image binaries."
    }
  }
}
